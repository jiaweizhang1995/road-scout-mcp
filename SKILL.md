---
name: road-scout
description: 周边旅行与本地发现。用户询问某地附近的小众景点、民宿、山野空间、本地体验、吃饭去处，或寻找与某个参考地点相似的地方时使用。通过 road-scout MCP 只读检索小红书、B站、抖音、公开网页和高德美食榜，并用 Jev 评估第一手体验信号、营销倾向和用户偏好匹配。
version: 1.5.0
---

# road-scout 周边发现

只读的旅行与本地发现能力。

默认使用 `road_scout_recommend` 完成完整推荐流程。低层搜索、正文、评论和 Jev 工具主要用于参考地点研究、定向补查和故障排查。

不要在 Agent 层重新实现服务端已经完成的搜索、筛选或排序逻辑。

## 核心原则

* 用户明确表达的需求和偏好优先于默认偏好。
* 用户没有明确偏好时，默认偏向：小众、人少、本地体验、适合自驾。
* 不要根据参考地点名称擅自推断“禅意”“治愈”“网红”“高级感”等风格；只有用户明确表达或原始证据明显支持时才使用。
* Jev 判断的是内容中的第一手体验信号、营销倾向和偏好匹配，不是作者身份认证，也不是事实真实性证明。
* 不只按点赞、收藏或热度排序。低互动内容只要正文有具体路线、价格、时间、停车、缺点、现场体验等细节，也可以优先。
* 不为了凑数量加入低质量候选。正式推荐不足时宁可少给。
* 原始证据中没有的信息不要补造。停车、营业时间、路线、门票等未得到支持时标记“未核实”。

## 1. 默认入口：`road_scout_recommend`

以下请求默认直接使用 `road_scout_recommend`：

* 附近去哪
* 周末自驾
* 小众景点
* 山野空间
* 民宿
* 本地体验
* 安静、人少、有意思的地方
* 本地吃饭
* 已经明确给出具体偏好的旅行发现

### 参数

`request`

保留用户原始需求，避免过度改写。

例如：

* `杭州附近100公里小众自驾`
* `想找安静、人少、有院子的民宿`
* `莫干山周边周末去哪`
* `杭州本地人常去吃什么`

`area_name`

用于提供明确的地理锚点，例如城市、区县、景区或地名。

如果用户问“附近”但当前上下文没有任何地点信息，需要先获得地点，不要猜城市。

`categories`

只传当前请求真正相关的类别。

例如：

* 户外：`["山野", "本地体验"]`
* 住宿：`["民宿"]`
* 普通周边发现：`["山野", "民宿", "本地体验"]`
* 纯吃饭：`["本地美食"]`

`preferences`

优先传用户明确表达的偏好。

例如：

* `["安静", "人少", "适合自驾"]`
* `["热闹", "公共交通方便", "夜生活"]`
* `["可以带狗", "有院子", "适合两天一夜"]`

不要因为系统默认偏好而覆盖用户要求。

`include_food`

用户明确询问：

* 吃什么
* 餐馆
* 本地美食
* 吃饭去哪里

时设为 `true`。

`max_results`

默认 5。

正式推荐数量只设上限，不要求必须填满。

## 2. 如何使用高层结果

`road_scout_recommend` 已经完成：

搜索 → 候选去重 → 小红书正文读取 → Jev 筛选 → 必要补查 → 最终排序

不要再在 Agent 层建立第二套评分公式。

主要关注：

### `recommendations`

正式推荐。

正常只包含：

* `supported`
* `marketing_risk`

优先用这里回答用户。

### `exploratory`

有潜力但证据不足的线索，通常对应：

* `insufficient`
* 无法完成 Jev 判断
* 正文读取失败
* 缺少关键证据

必须和正式推荐分开。

可以称为：

* 备选线索
* 值得继续查的地方
* 目前证据不足的候选

不要写成已经充分验证的推荐。

### `filtered`

已经被质量筛选淘汰。

不要重新根据标题或点赞把它放回推荐。

### `food`

高德美食榜候选。

纯美食请求应优先使用这里的结果，不要输出无关的山野、民宿推荐。

高德榜单代表候选价值，不代表：

* 当前一定营业
* 一定不用排队
* 一定有停车位
* 排名就是绝对质量

没有额外证据时明确提醒用户核实变化较快的信息。

### `source_status` / `notes`

用于理解来源覆盖。

不要机械向用户汇报每个平台是否成功。

只有以下情况才需要说明：

* 重要来源不可用，明显影响推荐质量
* 所有主要社交来源均失败
* Jev 没有完成筛选
* 用户明确询问数据来源或故障状态

## 3. “帮我找像 X 一样的地方”

这是普通推荐之外的特殊流程。

不要直接把“像 X”当成充分的偏好条件。

先理解参考地点为什么值得模仿。

### 流程

1. 使用 `social_search` 搜索：

   * `参考地点名称 + 所在区域 + 实际体验`

2. 如果有小红书结果：

   * 选择 1–3 条有代表性的结果
   * 使用 `xiaohongshu_note` 读取正文

3. 从用户表达和原始证据中提取少量明确特征。

例如：

* 临溪
* 山谷
* 老宅
* 有院子
* 茶空间
* 可以住宿
* 安静
* 游客少
* 适合散步
* 景观开阔

4. 将这些具体特征作为 `preferences`，再调用 `road_scout_recommend`。

5. `request` 中保留参考地点名称，方便检索保持语义关联。

如果无法获取参考地点的可靠证据，只使用用户自己明确说出的特征，不自行脑补风格。

## 4. 美食请求

### 纯美食请求

例如：

* 杭州吃什么
* 附近有什么好吃的
* 想找本地人吃的餐馆

调用：

* `categories=["本地美食"]`
* `include_food=true`
* 提供明确 `area_name`

回答时：

1. `food` 为主
2. 社交证据用于补充实际体验
3. 不输出无关的山野、民宿或景点结果

### 旅行 + 吃饭

例如：

`周末去莫干山玩，顺便找两家吃饭的`

保留旅行 categories，同时设置：

`include_food=true`

旅行推荐与餐馆候选分开呈现。

## 5. 证据应该如何理解

不同来源承担不同角色。

### 小红书正文

适合判断：

* 是否存在第一手体验叙述
* 路线
* 停车
* 价格
* 排队
* 实际优缺点
* 现场感受

正文比标题和点赞更重要。

### 小红书评论

用于补充或质疑原帖：

* 停车变化
* 门票变化
* 排队
* 封路
* 营业状态
* 踩坑
* 实际限制

评论不能脱离原帖单独当作确定事实。

多个评论也不自动等于多个独立可靠来源。

### B站 / 抖音

主要用于：

* 发现地点
* 近期现场氛围
* 补充视频线索

如果只有搜索摘要，不要给予和完整正文相同的证据强度。

### Web

适合：

* 官方信息
* 营业信息
* 路线和交通
* 公开资料
* 补充交叉验证

### 高德榜

适合发现餐馆候选。

不把榜单本身解释成“已经验证的用户口碑”。

## 6. Jev 信号

Jev 主要提供：

### `firsthand`

判断内容是否表现出第一手体验特征。

例如：

* 实际到达过程
* 路线
* 时间
* 停车
* 价格
* 排队
* 天气
* 路况
* 优缺点
* 踩坑

高 `firsthand` 不等于已经证明作者真实身份。

### `marketing`

判断：

* 广告
* 商业合作
* 团购
* 私信预订
* 购买链接
* 商务联系方式
* 商家自营
* 模板导流

营销内容不一定毫无价值。

如果存在合作，但同时提供大量可执行体验细节，可以保留为：

`marketing_risk`

### `fit`

只判断与本次用户偏好的匹配程度。

不要把 `fit` 当作内容真实性评分。

### 默认展示方式

普通用户回答不需要展示未经领域校准的精确概率。

优先翻译成：

* 有较强第一手体验支持
* 有一些实际体验细节
* 营销/合作倾向较高
* 与本次偏好匹配较好
* 证据不足

用户明确要求调试、评分或查看模型输出时，再展示原始 Jev 信号。

## 7. 什么时候使用低层工具

正常推荐不要重复调用低层工具。

以下情况才使用。

### `social_search`

用于：

* 研究参考地点
* 用户明确要求看某个平台
* 针对具体地点做额外搜索

### `xiaohongshu_note`

用于：

* 查看某篇具体笔记正文
* 参考地点研究
* 用户追问某个候选的原始体验

必须使用搜索返回的完整 signed URL。

不要根据 note ID 自行重建 URL。

### `xiaohongshu_comments`

只在某个关键问题可能改变推荐时使用。

例如：

* 停车
* 门票
* 排队
* 路况
* 营业
* 是否闭店
* 评论存在明显争议

不要对所有候选无差别拉评论。

### `douyin_search`

用户特别在意近期现场、视频或氛围时使用。

### `douyin_creator_comments`

只有已经拥有可靠 `sec_uid` 时使用。

没有就不要猜。

### `jev_rank_candidates`

只在手工收集了一组候选、需要单独重新评估时调用。

正常 `road_scout_recommend` 已经内置 Jev。

### `gaode_food_ranking`

需要单独补查餐馆榜时使用。

### `nearby_discover`

旧的底层 evidence 收集工具。

正常用户推荐不优先使用。

### `road_scout_status`

仅用于：

* 首次故障诊断
* 工具调用失败
* 用户明确询问连接状态

不要正常每次推荐都调用。

## 8. 最终回答规范

普通情况下输出 3–5 个正式推荐。

每条推荐优先包括：

* 名称
* 为什么符合这次需求
* 来源平台
* 原始链接
* 1–2 条关键证据
* 主要风险或限制

实用信息遵循：

* 有证据 → 提供
* 有冲突 → 说明冲突
* 没证据 → 写“未核实”
* 不允许根据常识补造

例如：

正确：

`原帖提到村口可以停车，但收费情况未核实。`

不要写：

`村口有免费停车场。`

如果用户没有要求，不要把回答变成 Jev 调试报告。

## 9. `exploratory` 的使用

如果正式推荐不足，但存在 `exploratory`：

可以增加一个很短的“备选线索”。

例如：

`另外还有两个地点看起来符合你的方向，但目前正文/实用信息不足，我先不把它们算正式推荐。`

不要把它们与正式推荐混合排序。

如果正式推荐为空：

直接说明当前没有筛到足够可靠的推荐。

不要为了必须回答几个地点而降低门槛。

## 10. 降级规则

### Jev 不可用

可以展示少量未排序候选或 exploratory，但明确：

`这次没有完成 Jev 质量筛选。`

不要自行假装完成营销过滤。

### 小红书正文读取失败

保留其他来源。

不要把：

* 标题
* 点赞量
* 搜索摘要

伪装成完整正文证据。

### 评论失败

保留原帖。

评论缺失本身不是负面信号。

### 抖音失败

继续使用：

* 小红书
* B站
* Web

不要中断整次推荐。

### 高德失败

纯美食请求可以使用其他来源作为备选，但明确榜单暂不可用。

不要伪造餐厅排名。

### 社交来源为空但 `food` 有结果

纯美食请求仍正常返回餐馆候选。

## 11. 只读边界

允许：

* 搜索
* 阅读正文
* 阅读评论
* 使用用户已经存在并明确控制的 Chrome 登录会话

禁止：

* 发帖
* 评论
* 点赞
* 收藏
* 关注
* 修改用户账号
* 主动执行登录
* 导出 Cookie
* 将 Cookie、API key、Bearer token 等凭据展示给用户

signed URL 只用于读取和来源追踪。

不要单独提取、解释、保存或传播其中的签名参数。
